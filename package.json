{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.17.1",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/daily-expense dev",
    "build": "pnpm run typecheck && pnpm --filter @workspace/daily-expense run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "ogl": "^1.0.11"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}